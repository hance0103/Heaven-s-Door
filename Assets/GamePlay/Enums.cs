namespace GamePlay
{
    public class SystemEnum
    {
        public enum eSellState
        {
            Wall,
            Empty,
            Filled
        }

        public enum eNodeState
        {
            None,
            Moveable,
            Drawable,
        }

        public enum eNodeDirection
        {
            Up,
            Down,
            Right,
            Left
        }
    }
}
